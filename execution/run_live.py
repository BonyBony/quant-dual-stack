python import time, pandas as pd, datetime as dt, pathlib def main(): while True: try: sig_path = max(pathlib.Path("data").glob("*signals*.csv")) except ValueError: time.sleep(60); continue # TODO: insert Backtrader run or broker API call here pd.DataFrame({"ts":[dt.datetime.utcnow()], "eq":[0]}).to_csv("data/pnl.csv", mode="a", header=False, index=False) time.sleep(60) if __name__ == "__main__": main()