*** Begin Patch
*** Update File: research/strategies/macd.py
@@
     def position(self, df_hist: pd.DataFrame, params: MACDParams) -> int:
         """
         Position at the last bar of df_hist:
           +1 long, -1 short, 0 flat
         """
         if df_hist.empty:
             return 0
-        c = df_hist["close"].astype(float)
-        macd = c.ewm(span=params.fast).mean() - c.ewm(span=params.slow).mean()
-        signal = macd.ewm(span=params.signal).mean()
-        hist = macd - signal
-        pos = (hist > 0).astype(int) - (hist < 0).astype(int)
-        # scalar-safe (avoid FutureWarning on int(Series))
-        return int(pos.iat[-1]) if len(pos) else 0
+        c = df_hist["close"].astype(float)
+        macd = c.ewm(span=params.fast, adjust=False).mean() - c.ewm(span=params.slow, adjust=False).mean()
+        signal = macd.ewm(span=params.signal, adjust=False).mean()
+        hist = macd - signal
+        # Deadband: ignore signals smaller than cost; cost will be set externally on the strategy
+        threshold = getattr(self, "deadband_threshold", 0.0)
+        if abs(hist.iat[-1]) < threshold:
+            return 0
+        pos = (hist > 0).astype(int) - (hist < 0).astype(int)
+        return int(pos.iat[-1]) if len(pos) else 0
 
*** End Patch
*** End Patch
*** Begin Patch
*** Update File: research/backtesting/wf_cpo.py
@@ class WF_CPOBacktester:
     def run(self, cfg: WF_CPO_Config) -> Tuple[pd.Series, pd.DataFrame]:
@@
         # OOS: daily param selection and trading
-        strat = MACDStrategy()
+        strat = MACDStrategy()
+        # derive cost-per-turn from commission+slippage for gates and hysteresis
+        round_trip_cost = self.cost.commission_pct + self.cost.slippage_pct
+        # set deadband threshold on the strategy (cost gate)
+        strat.deadband_threshold = round_trip_cost
 
-        pos_prev = 0
-        rets = []
-        dates = []
-
-        # ensure we have enough history to start decisions
-        start_idx = close.index.get_loc(test_days[0])
-        i = max(start_idx, max_slow)  # index in close
-        while i + 1 < len(close) and close.index[i] <= test_days[-1]:
-            d = close.index[i]
-            # features as of day d (no peeking)
-            feats_today = compute_features_daily(df.loc[:d], cfg.lookbacks)
-            best = select_params_for_day(model, feats_today, pg_dicts)
-            p = MACDParams(**best)
-
-            # decide today's position per strategy
-            hist_i = pd.DataFrame({"close": close.iloc[:i+1]}, index=close.index[:i+1])
-            pos_new = strat.position(hist_i, p)
-
-            # trade to tomorrow
-            pnl = self._day_ret_with_costs(close, pos_prev, pos_new, i)
-            rets.append(pnl)
-            dates.append(close.index[i+1])
-
-            # update position after trade
-            pos_prev = pos_new
-            i += 1
-
-        rets = pd.Series(rets, index=pd.DatetimeIndex(dates), name="return")
+        pos_prev = 0
+        pred_prev = 0.0  # previous predicted net return
+        rets = []
+        dates = []
+
+        # ensure we have enough history to start decisions
+        start_idx = close.index.get_loc(test_days[0])
+        i = max(start_idx, max_slow)  # index in close
+        while i + 1 < len(close) and close.index[i] <= test_days[-1]:
+            d = close.index[i]
+            # features as of day d (no peeking)
+            feats_today = compute_features_daily(df.loc[:d], cfg.lookbacks)
+
+            # compute predicted returns for each param and pick best
+            preds = predict_grid(model, feats_today, pg_dicts)
+            best_param = preds.idxmax()
+            pred_best = preds.max()
+
+            # hysteresis: only switch if the improvement exceeds the cost
+            chosen = best_param if (pred_best - pred_prev) > round_trip_cost else tuple(sorted(p.__dict__.items()))
+
+            p = MACDParams(**dict(chosen))
+
+            # decide today's position per strategy
+            hist_i = pd.DataFrame({"close": close.iloc[:i+1]}, index=close.index[:i+1])
+            pos_new = strat.position(hist_i, p)
+
+            # if predicted return <= cost → stay flat (gate)
+            if pred_best <= round_trip_cost:
+                pos_new = 0
+
+            # trade to tomorrow
+            pnl = self._day_ret_with_costs(close, pos_prev, pos_new, i)
+            rets.append(pnl)
+            dates.append(close.index[i+1])
+
+            # update for next iteration
+            pos_prev = pos_new
+            pred_prev = pred_best
+            i += 1
+
+        rets = pd.Series(rets, index=pd.DatetimeIndex(dates), name="return")
 
         # save OOS params (optional: last-day choice only for quick reference)
*** End Patch
*** End Patch

